{
  "name": "my-project",
  "version": "1.0.0",
  "description": "Some project with Vue2 and HumanUI",
  "author": "mmper <mmper@foxmail.com>",
  "private": true,
  "scripts": {
    "start": "node ./scripts/development.js",
    "dev": "node ./scripts/development.js",
    "build": "node ./scripts/production.js",
    "git": "git init && git add . && git commit -m 'init project'",
    "first": "yarn install && yarn run git"
  },
  "dependencies": {
    "@freshservice/bridge": "^1.2.8",
    "vue-human": "^2.8.2"
  },
  "devDependencies": {
    "@freshes/house": "^1.2.0"
  },
  "engines": {
    "node": ">= 8.9.0",
    "npm": ">= 5.5.0"
  }
}
